# This file is used to specify the Python packages that are required to run
# this project.
#
# You can install all the required packages by running the following command:
# pip install -r requirements.txt
#
# Add your project's dependencies below, one per line.
# For example:
#
# numpy==1.26.4
# pandas>=2.0.0

# As of now, this project has no external dependencies.
