# This file is used to specify the Python packages that are required to run
# this project.
#
# You can install all the required packages by running the following command:
# pip install -r requirements.txt
#
# Add your project's dependencies below, one per line.
# For example:
#
# numpy==1.26.4
# pandas>=2.0.0

# Install the current project in "editable" mode. This makes the
# 'cyberprompt' package available for import in the environment, which is
# essential for running tests.
-e .
